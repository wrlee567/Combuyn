# Render blueprint — FastAPI backend + managed Postgres.
# The React frontend deploys separately on Vercel (see frontend/vercel.json).
services:
  - type: web
    name: combuyn-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: combuyn-db
          property: connectionString
      # Seed immutable reference data (frameworks, controls, ISO 42001 catalog).
      - key: SEED_ON_STARTUP
        value: "true"
      # Never seed sample/demo records in production.
      - key: SEED_DEMO_DATA
        value: "false"
      # Schema is managed by Alembic migrations (run before app start), not create_all.
      - key: AUTO_CREATE_SCHEMA
        value: "false"
      - key: ENVIRONMENT
        value: production
      # Signing secret for JWT bearer auth — generated once by Render.
      - key: JWT_SECRET
        generateValue: true
      # Set this to your Vercel frontend origin after the first deploy.
      - key: CORS_ORIGINS
        sync: false

databases:
  - name: combuyn-db
    databaseName: combuyn
    user: combuyn
    plan: free
